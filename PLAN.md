# План реализации v1

Спутник `SPEC.md`. Спека говорит, что построено; план — в каком порядке и чем
проверено. Порядок этапов — по убыванию риска, не по слоям.

**Номер этапа — это его имя, а не его очередь.** Этапы 0–5, 7 и С закрыты;
Linux-ядро этапа 8 (§6.8 и реальный TUN) тоже закрыто. Текущий порядок:

```
6 → 8 (остаток) → 9
```

где **С** — этап связки, которого в первой редакции плана не было вовсе (см.
D12). Причина порядка — та же, что уже один раз переставила этапы 4 и 5: писать
нужно там, где есть на чём писать тесты. Резолвер этапа 6 обязан резолвить
«через активный узел» (§5.7), а активного узла до стора и связки не существует,
— поэтому 7 и С были закрыты перед DNS. Теперь `hop up` поднимает реальный Linux
TUN, трафик идёт через выбранный узел, каждый исходящий сокет агента привязан к
текущему физическому интерфейсу, а перехваченный :53 обслуживает настоящий
резолвер §5.7, а разрыв активных соединений при смерти узла (§5.5) рвёт их по
настоящему реестру netstack, а не только колбэком в тесте. `BypassSink` тоже
подключён к настоящему NAT-пути (§6.10, §6.8, `internal/bypass`) — вердикт
`bypass` больше не дропает пакет молча, тот класс лжи, что был у `Interrupt`,
закрыт. Списки bypass/block §6.10 перестали быть кодом: `netstack.Routing` —
значение конфигурации, и `verdict.go` только спрашивает у него «совпало ли».
Остались L2-хвост этапа 6, платформенный и конфигурационный хвост этапа 8 и CLI.
Самая дорогая из оставшихся дыр — TCP в локальную сеть, который вердикт `bypass`
до сих пор дропает молча вместо RST.

Сквозное правило: **оракул раньше функциональности.** Поддельный `PacketDevice`
(§8.1), fault injector, тестовые VLESS-инбаунды и инъектируемые часы строятся до
логики, которую они проверяют. Каждый этап закрывается зелёными тестами из §8,
названными по номерам.

**Правило ведения:** после каждого законченного шага обновлять здесь статус,
фактическое решение и следующий незакрытый риск. `HANDOFF.json` при этом хранит
только ближайшее действие, а не копию всей истории плана.

---

## 0. Переработка §6.2 — teardown и watchdog

§6.2 писался, когда граница между процессами была управляющей. После §5.3 та же
граница несёт данные, и это меняет разбор: **соединение датаплейна само по себе
есть сигнал живости**, причём точный, а heartbeat — приближение к нему.

Конфликт не в том, что T23 и T24 хотят разных чисел. Он в том, что один порог
отвечает сразу на два разных вопроса:

1. «Должен ли туннель существовать?» — от этого зависит teardown (T24 хочет
   длинный порог).
2. «Обслуживает ли кто-то пакеты прямо сейчас?» — от этого зависит судьба
   пакета, пришедшего в эту миллисекунду (T23 хочет короткий).

Одним таймаутом можно ответить только на один. Отсюда дыра: внутри окна
интерфейс жив, маршруты на месте, читателя нет, кольцо переполняется, пакеты
**молча** дропаются. Это ровно тот отказ, против которого написан §5.6
(«Закрытие — отказ, а не молчание»), и воспроизводится он не по ошибке
реализации, а по конструкции §6.2.

Референса нет: у hiddify watchdog отсутствует вовсе — сервис живёт до явного
`Stop`, единственные таймауты в `tunnelservice/admin_service_commander.go` это
1/5/20 с на самих gRPC-вызовах.

### Решение: одно ребро + один таймер + responder на TUN

Состояния сервиса: **`up → orphaned → (up | down)`**.

**Ребро в `orphaned` — 0 мс, не по таймеру.** ОС сообщает о смерти агента
мгновенно и в том числе при `kill -9`: ядро закрывает дескрипторы, сервис
получает EOF/EPIPE на управляющем сокете и `ERROR_BROKEN_PIPE` на трубе данных.
Heartbeat для обнаружения смерти процесса не нужен вообще.

**Что делает `orphaned`.** Интерфейс, адреса, маршруты и индекс не трогаются —
это то, что нужно T24. Сервис остаётся читателем TUN и немедленно отвечает на
пакеты TCP RST или ICMP unreachable; молчаливого окна нет.

| ОС      | механизм                                                | что видит приложение   |
| ------- | ------------------------------------------------------- | ---------------------- |
| Linux   | сервис читает TUN и синтезирует RST/ICMP                | RST / ICMP unreachable |
| macOS   | тот же общий responder поверх платформенного устройства | RST / ICMP unreachable |
| Windows | сервис дренирует кольцо Wintun тем же responder'ом      | RST / ICMP unreachable |

Построение ответов общее для трёх ОС и живёт в `internal/reject`; привилегия
нужна сервису для владения устройством, но не создаёт отдельной платформенной
ветки политики отказа. Вариант с reject-маршрутами был отвергнут живым тестом:
он не прерывает уже установленные TCP-соединения и оставляет молчаливое окно.

Исключения §5.6 (RFC1918 и аналоги, DHCP, NTP) в `orphaned` сохраняются — иначе
машина теряет DHCP-аренду ровно в окне, когда она её продлевает.

**Таймер один: `orphan_deadline`.** Пришёл новый агент и предъявил attach-token
в пределах дедлайна — сервис возвращает нормальные маршруты и отдаёт тот же fd /
ту же трубу. Не пришёл — полный teardown до снапшота.

Стартовые значения: `orphan_deadline = 15 с` (перекрывает respawn
systemd/launchd/SCM плюс реконнект с запасом), `heartbeat = 1 с`,
`heartbeat_miss = 3`.

**Роль heartbeat после этого.** Он больше не детектор смерти процесса. Он —
детектор **заклинившего живого агента** (дедлок, паника netstack, из которой
процесс вышел в нерабочее состояние): сокет там не закрывается, ребро не
срабатывает. 3 пропуска = то же самое ребро в `orphaned`. Оба сигнала кормят
одну машину состояний.

`Detach(reason=restart)` перед плановым рестартом входит в то же безопасное
состояние `orphaned`; быстрый reattach лишь сокращает время работы responder'а.

### Как разводятся T23 и T24

Они перестают спорить, потому что измеряют разные свойства, а не разные значения
одного порога.

- **T23-fast** (короткое): `kill -9` → в пределах ≤ 1 с TCP-connect наружу
  получает RST/`EHOSTUNREACH`, а не висит. Свойство _безопасности_, не зависит
  ни от какого таймера — оно на ребре.
- **T23-slow** (длинное): в пределах `orphan_deadline + ε` (≤ 20 с) туннель
  снят, снапшот сети совпал побайтово. Свойство _уборки_. Длинный порог теперь
  безвреден, потому что окно больше не молчаливое.
- **T23b** (в спеке отсутствует): агент умер и не вернулся — на всём протяжении
  окна TCP даёт RST, UDP даёт ICMP port unreachable. Именно этот тест сегодня
  невозможно написать, и именно он ловит нарушение §5.3 и §5.6.
- **T24**: агент убит и поднят внутри `orphan_deadline` → индекс и имя
  интерфейса не менялись, маршруты вернулись в туннельную форму, переиспользован
  тот же дескриптор. Плюс подпроверка: в зазоре трафик **отказывался, а не
  висел** — тот же механизм, что в T23-fast, другой исход.

T23 и T24 больше не делят одно число: T23-fast упирается в ребро (0), T23-slow и
T24 — в один и тот же `orphan_deadline`, и оба зелены при любом его значении в
разумном диапазоне.

---

## Этап 0. Интерфейсы и скелет оракула

**Что делается.** `PacketDevice` ровно как в §3.2. `FakePacketDevice` — пара
буферов плюс `Inject` / `Emitted` / `ExpectRST` / `ExpectICMPUnreach`. `Clock`
(Now/After/NewTicker) и фейковые часы с ручной прокруткой. `Prober` как
интерфейс. **Реестр флагов отключения политик и негативная CI-задача из §8** — с
первого дня, не ретрофитом.

**Чем машина проверяет.** `go build ./...` на трёх ОС. Линт, запрещающий
`time.Now`/`time.After` вне пакета часов. Мета-проверка: в реестр кладётся
заведомо плохой тест (зелёный при выключенной логике) — негативная задача
обязана его покраснить.

**Что должно сломаться при выключении.** Ломать нечего; ломается сам оракул,
если мета-проверка не сработает.

**Что способен опровергнуть.** Тезис «флаги отключения дёшевы» — если флаг
потребует протаскивания конфига в каждый call-site, §8 придётся переписать на
другой механизм (build tags вместо рантайм-флагов).

---

## Этап 1. B1 — ворота (первым, до всякого кода на границе)

**Что делается — минимальная реализация, достаточная для замера.** Netstack,
Xray, health, сервис — ничего этого нет.

- **Windows:** пара named pipe (по одной на направление, чтобы полудуплекс не
  душил встречный поток), **byte-режим с явным length-prefix framing и
  батчингом**, не message-режим. Message-режим сохраняет границы, но стоит один
  syscall на пакет; framing даёт N пакетов на один `WriteFile`. Чтение — крупный
  буфер (256 KiB), нарезка на `[][]byte` без копирования внутри батча. Транспорт
  — `go-winio` (MIT, overlapped I/O через IOCP); если упрётся — запасной замер
  на голом overlapped `ReadFile`/`WriteFile`.
- **Linux/macOS:** дескриптор. На Linux — реальный TUN-fd в netns; на macOS —
  `utun`; плюс контрольный замер на `socketpair` как чистая стоимость syscall.
- Харнесс: 1400 байт, одно- и двунаправленно, батчи {1, 8, 32, 64, 128}, отчёт
  pps + Мбит/с + CPU.

**Два замера, а не один.** B1 в §8.6 меряет только трубу, но реальный путь на
Windows добавляет ещё один memcpy: `ReceivePacket` отдаёт срез в кольцо Wintun,
который надо освободить, то есть скопировать до записи в трубу. Меряем:

- **B1a** — только труба;
- **B1b** — копия из кольца + труба. **Гейтом §5.3 служит B1b**, честное число.

### Гейт — только Windows

**B1b на Windows, порог 500 Мбит/с — единственный порог, ломающий сборку.**
Linux и macOS меряются и записываются как базовая линия **без порога**.

Причина. На Unix границы в горячем пути нет: агент читает дескриптор ровно так
же, как это делает wireguard-go. Батч-API там пустышка без GSO —
`tun_linux.go:451` без `vnetHdr` возвращает ровно `1` пакет за `read()`, а
`tun_darwin.go:306` `BatchSize()` захардкожен в `1`. Единственный способ
получить больше одного пакета за syscall — `IFF_VNET_HDR` + `TUNSETOFFLOAD`, то
есть GSO, который §4 исключает явным списком. На macOS аналога нет вовсе.

Отсюда: 2 Гбит/с на Unix гейтит не §5.3, а скорость gvisor плюс syscall'ов, то
есть вопрос производительности на потом. Держать это гейтом сборки опасно ровно
тем, что проект упрётся в оптимизацию, которую §4 сам же вынес за v1.

**Дополнительно:** контрольный замер с `IFF_VNET_HDR` — чтобы знать цену
возврата GSO, если он однажды понадобится. Число записывается, порогом не
становится.

**Чем машина проверяет.** B1b Windows ≥ 500 Мбит/с как гейт. B1a/B1b
Linux/macOS + контрольный GSO-замер — в отчёт. Тест фрейминга: батч из N пакетов
читается как ровно N пакетов побайтово.

**Что должно сломаться при выключении.** Выключаем length-prefix framing → тест
фрейминга краснеет (склейка/разрыв пакетов детектируется). Бенчмарк сам по себе
не тест — у него порог.

**Что способен опровергнуть.** Абзац «Цена» в §5.3. Ориентир: 500 Мбит/с при
1400 байт = ~44 600 pps; при батче 32 это ~1400 записей в секунду, запас
большой. Реальный риск не в пропускной способности, а в латентности, джиттере и
CPU — их и надо записать в отчёт.

### Если порог на Windows не взят

Падает **только цена, не причины.** Аргументы §5.3 — UDP full-cone против
TCP-якорей socks-инбаунда (`proxy/socks/server.go:177`) и устранение второго
netstack — от пропускной способности не зависят. Варианты по возрастанию цены:

1. Крутить транспорт: больше батч, две трубы, увеличенный буфер трубы, голый
   overlapped вместо go-winio. Архитектура не двигается.
2. **Заменить трубу на shared memory ring + пара событий**
   (`CreateFileMapping` + auto-reset events) — то, что делает сам Wintun.
   `PacketDevice` не меняется ни на строку; ради этого абстракция и вынесена.
   Самый сильный запасной вариант.
3. Отдать владение адаптером Wintun агенту на Windows — но открытие адаптера
   требует прав, то есть это отказ от §5.2 на одной ОС.
4. Крайний: перенести netstack в сервис на Windows. Это убивает §3.2 («выше
   абстракции ОС-зависимого кода нет») и заново открывает вопрос SOCKS. **Только
   этот вариант реально ломает §5.3.**

---

## Этап 2. Сервис, машина состояний `orphaned`, teardown

**Что делается.** Управляющий IPC (§3.1) плюс `Detach` / `Attach(token)`.
Передача fd через `SCM_RIGHTS`, труба с SDDL на Windows. Машина состояний из
раздела 0: ребро по закрытию соединения, TUN-responder, `orphan_deadline`,
реаттач. Снапшот и восстановление состояния сети — самостоятельный тестируемый
объект, а не побочный эффект `down`.

**Чем машина проверяет.** T22, T23-fast, T23-slow, **T23b**, T24, **T29**. Плюс
чистый тест машины состояний на фейковых часах — идёт как L1, без прав, на всех
трёх ОС, и ловит большую часть регрессий до платформенных раннеров.

**T29 (новый): смерть самого сервиса.** `up`, затем `kill -9` сервиса, затем
снапшот. D1 закрывает смерть агента, но смерть сервиса не покрыта ни решением,
ни тестом. Измерение на Linux показало: интерфейс уходит, а правила Hop с
приоритетами 31000 и 32000 переживают смерть сервиса. Старое правило 31500 с UID
могло остаться только от предыдущей реализации; новый сервис его не создаёт, а
переходный `Reclaim` удаляет. На macOS и Windows проверяется то же самое плюс
настройки DNS, которые могут пережить процесс.

**Что должно сломаться при выключении.** `orphan_reject=off` → T23-fast и T23b
виснут вместо RST. `orphan_deadline=0` → T24 краснеет (интерфейс исчезает).
`snapshot_restore=off` → T22 краснеет.

**Что способен опровергнуть.** Тезис, что привилегированная поверхность остаётся
маленькой: если responder разрастётся за пределы построения RST/ICMP, придётся
признать, что §5.2 стоит дороже заявленного. T29 уже доказал отдельную
обязанность §6.2: при старте сервис должен уметь вернуть оставшиеся после его
смерти правила маршрутизации в исходное состояние.

---

## Этап 3. Netstack поверх фейкового PacketDevice

**Что делается.** gvisor `tcpip` на `PacketDevice`; вердикт на первом пакете в
порядке §3.4; reject через RST/ICMP; bypass-список §6.10; UDP full-cone с NAT по
source addr:port. Xray ещё нет — исходящее подменено фейковым диалером.

**Чем машина проверяет.** T12, T13, T15, T16, T17 — все L2, без прав, на трёх
ОС. Плюс **B3**.

**Что должно сломаться при выключении.** `verdict_order=bypass_first` → T16 (DNS
на `192.168.1.1:53` уходит в локальную сеть). `reject_mode=drop` → T12/T13 ждут
таймаута. `nat_key=src+dst` → T15 теряет ответы, B3 показывает рост таблицы.

**Что способен опровергнуть.** Выбор gvisor как netstack (§5.1): если получение
per-flow вердикта до установления соединения окажется в gvisor дорогим или
потребует форка, придётся либо писать свой стек, либо менять место вердикта.

---

## Этап 4. Движок Xray, fault injector, реальный VLESS

**Что делается.** Генерация конфига, `core.Dial` / `core.DialUDP`
(`core/functions.go:49`, `:76`), один outbound на узел, классификация ошибок
§6.15 как замкнутое перечисление **в одной-единственной функции**, оборачивающей
dial. Fault injector на 5 режимов, VLESS-инбаунды Xray в том же процессе,
управляемые пробе-таргеты.

**Чем машина проверяет.** T20. Самотесты инжектора: каждый режим даёт ожидаемое
наблюдаемое (`rst` → ECONNRESET, `blackhole` → ноль байт и нет закрытия,
`cut-after-handshake` → закрытие после N байт). Линт: `health.ReportFailure` не
вызывается ниоткуда, кроме этой одной функции.

Сюда же переезжает **весь L2-хвост этапа 5**, писать который раньше не на чем:
T9, T10, T11 (разрыв по причине, таймаут blackhole), A34 и A35 — доказательство
§6.7, что проба идёт тем же путём, что и трафик, а не только получает тот же
`node_id`, — и **B2**. Реализация `health.Target` поверх настоящего HTTP через
outbound узла появляется здесь же; семантика сведения URL (§5.4) от неё не
зависит и уже проверена на L1.

**Что должно сломаться при выключении.** `error_classify=all_failures_kill` →
T20 краснеет: 502 от сайта убивает живой узел.

**Что способен опровергнуть.** Полноту перечисления §6.15 — если реальная ошибка
`core.Dial` не ложится ни в один из четырёх видов, перечисление неверно и
правится по факту, а не по замыслу. Также §6.7: если проба через тот же outbound
окажется дорогой, соблазн срезать угол проявится здесь.

**Что опроверг на самом деле.** Не полноту перечисления, а место классификации.
`core.Dial` не отдаёт отказ вовсе: соединение ленивое, а отказ приезжает на
первом вводе-выводе — и приезжает текстом `io: read/write on closed pipe`,
одинаковым для мёртвого узла и для мёртвого сайта. Вердикт пришлось поднять
уровнем ниже, на перехваченный системный диалер Xray, где узел отличим от цели
по построению. Из трафика наблюдаем только `DialTimeout`; §6.15 дописан.

Вторая находка — лицензионная: Xray-core под MPL-2.0 линкует
`github.com/sagernet/sing` под GPL-3.0, и обойти это нельзя (§5.1, вопрос §7.1).

---

## Этап 5. Health, выбор, переключение

**Что делается.** k-из-n (§6.3), гистерезис tolerance (§6.4), ярусное расписание
проб (§6.5), форс-проверка на событиях (§6.6), сигнал переключения с `reason`
(§5.5), разрыв соединений только при `dead`.

**Порядок — от верификации к коду.** Регистр `docs/verification-autoswitch.md`
написан до модуля и задал не только проверки, но и требования к наблюдаемости:
снимок целиком, счётчик завершённых раундов, синхронные `ReportFailure` / `Pin`
/ `Auto`. Шесть решений, которых в спеке не было (воскрешение, счётчик трафика,
сведение URL, холодный старт, стартовый fail-close, буквальность фиксации),
приняты там же и переехали в §5.4, §5.6, §6.3–§6.5, §1/С3.

**Чем машина проверяет.** L1 — T1–T8 и A1–A33 на фейковых часах и фейковом
пробере, `internal/health` (сделано). L2 — T9, T10, T11, T20, A34, A35 через
реальный VLESS и инжектор: **переезжают в этап 4**, раньше их написать не на
чем. Там же **B2**; его порог совпадает с A25, который считает ту же величину по
расписанию.

**Что должно сломаться при выключении.** Семь флагов вместо пяти. `k_of_n=1` →
T2, A3. `tolerance=0` → T3. `switch_reason=off` (рвём всегда) → A19 на L1 и T10
на L2. `forced_probe=off` → T6, A24. `multi_url=off` → T5, A31.
`probe_tiers=off` → A25, A26. `traffic_kills=off` → A5, A29.

Три проверки регистра остались без флага намеренно: A10 проходит и при нулевом
`tolerance`, A14 не зависит от `reject_mode`, A28 при `k=1` только ускоряется.
Проверка без флага — норма; флаг без краснеющей проверки — нет, и это ловит
`negcheck`.

**Что способен опровергнуть.** Стартовые значения §6.3 и §6.4: T3 на 20 циклах с
джиттером 80–120 мс прямо покажет, хватает ли 50 мс. §6.5 проверяется B2
численно — и уже опровергнут один раз при написании регистра: хвост в 300 с
давал 2300 проб в час на 200 узлов, то есть «стоимость ограничена» не
выполнялось; отсюда 1800 с в §6.5.

---

## Этап 6. DNS — закрыт на L1

**Что делается.** Реальный резолвер за перехватом :53 (порядок вердиктов стоит с
этапа 3). Апстрим — UDP через `core.DialUDP` с повтором по TCP на флаге TC; два
адреса с форой второму 150 мс. Кэш с TTL, сброс по подписке на события `health`,
а не по вызову из связки. Bootstrap — отдельный резолвер мимо туннеля со своим
долгим кэшем. Его TCP- и UDP-сокеты, как и все исходящие сокеты агента, обязаны
создаваться через `internal/outbound.Selector` и привязываться к текущему
физическому интерфейсу на всех ОС; непривязанный fallback запрещён.

**Регистр уже написан:** `docs/verification-dns.md`. Порядок реализации из его
§7: парсер → один UDP-апстрим → второй с форой 150 мс → TCP retry на TC → кэши →
фазы и проводка в агент. Затем события, AAAA/ECS, bootstrap и оставшиеся L2.

**Чем машина проверяет.** T14; T16 перегоняется против настоящего резолвера.
Плюс L2 на усечённый ответ: апстрим отдаёт TC, резолвер обязан повторить по TCP
и вернуть полный RRset.

**Что должно сломаться при выключении.** `dns_cache_flush_on_switch=off` → T14
(подписка на события снята). `bootstrap=off` → стартовый тест уходит в петлю
§5.7(а). `dns_tcp_retry=off` → тест усечённого ответа отдаёт обрезанный RRset.
`dns_upstream=single` → тест, где первый апстрим мёртв, теряет резолв целиком.

**Что способен опровергнуть.** Стык §5.7(б) с этапом 2: в состоянии `orphaned`
DNS тоже обязан отказывать, иначе приложения получают адреса и виснут на
connect. Это проверяется вместе с TUN-responder'ом, а не предполагается из
поведения маршрутов.

**Что вышло.** Пять новых пакетов и подпакетов: `internal/dnsmsg` (разбор по
смещениям — своё, не `x/net/dns/dnsmessage`, потому что У6 требует копирования
байт, а смещений библиотека не отдаёт), `internal/dnstest` (L1-стенд: поддельный
апстрим, программируемые задержки на фейковых часах, счётчики трёх диалеров
раздельно), `internal/phase` (тип фазы, вынесенный из связки ради разрыва цикла
импорта), `internal/resolver` (апстримы, кэш, фазы, bootstrap) и переработанный
DNS поверх TCP в `internal/netstack`. Все десять флагов политик заведены и
проверены `negcheck` в обе стороны.

Регистр нашёл больше, чем ожидалось от него, и в двух местах разошёлся с уже
написанным кодом — Р27 (сброс кэша: §5.7 против Р33) и Р28 (транспорт клиента
против замороженного интерфейса); оба разрешены в `implementation-notes.md`.
Ревью волны 1 нашло четыре Important, из которых два ломали то, на чём стояли
следующие задачи: `Reply` брала секцию вопроса у апстрима вместо клиента (D30,
Р23), а сборка отказов паниковала на нулевом `Msg` — при том, что `Parse` при
ошибке возвращает именно его, и паника в горутине сняла бы туннель на пакете от
любого локального процесса.

**Чего в этом этапе нет.** Шаг 10 регистра: T14 и T16 против настоящего
резолвера на L2 и усечённый ответ через настоящий узел. И строка D34б,
заведённая по итогам реализации: усечённый ответ уходит клиенту без OPT.

---

## Этап 7. Подписки, стор, слияние — закрыт

**Что делается.** Каскад разбора §6.12 как упорядоченный набор распознавателей,
ключ слияния §6.16 с таблицей `user_id` по протоколам, diff-обновление §5.8,
`supported: false` и `unsup_reason` §6.11, права 0700/0600 §6.14. Стор — три
файла §2 с атомарной записью через `rename`; живость персистится срезом, без
окна. Полный `Node` живёт здесь, `engine.Node` и `health.Node` — проекции.

**Порядок внутри этапа — регистр раньше кода.** `docs/verification-store.md`
написан до модуля, по образцу `docs/verification-autoswitch.md`: шесть
утверждений, 39 проверок (T18, T19 плюс S1–S39), требования к наблюдаемости и
восемь решений, которых в спеке нет (Р7–Р14, нумерация продолжает регистр
автопереключения). Ожидание оправдалось — регистр нашёл больше, чем список
кандидатов, с которого начинался: недокачанная подписка не применяется вовсе,
слияние не пересекает границу группы, в `manual` слияния нет вовсе, у стора
появляется замок, потому что решение о персистентности живости само создало
гонку двух писателей.

**Чем машина проверяет.** Регистр целиком; порядок реализации — его §8.
Ключевое: golden-корпус с проверкой каждой нормализации §6.12 **и каждой ступени
каскада** по отдельности, инъекция отказа записи между `fsync` и `rename`, и шов
S37 — восстановленный `alive` не должен выдавать себя за проверенный, иначе
fail-close §5.6 не наступит там, где обязан.

**Что должно сломаться при выключении.** Шесть флагов: `merge_key` → T18, S1;
`merge_key_userid` → S3; `parse_cascade` → S22–S24; `atomic_write` → S25, S26;
`store_lock` → S28; `health_slice` → S36, S37. Обещанные планом два режима
одного `merge_key` разведены на два флага: `HOP_DISABLE` двоичный, и одним
флагом одна из двух проверок осталась бы без охранника.

**Что способен опровергнуть.** Порядок каскада §6.12 и сам ключ §6.16 — если
реальные подписки переиспользуют один `user_id` на разных серверах, ключ надо
менять. Отдельно — таблицу `user_id` §6.16: shadowsocks с одинаковым методом и
паролём на разных портах одного сервера различится, а вот два узла, отличающиеся
только полем, которого в таблице нет, склеятся, и это выяснится только на
настоящих подписках.

---

## Этап С. Связка агента — закрыт

Этапа с таким номером в первой редакции плана не было, и это была её главная
дыра: шесть проверенных модулей не соединял никто, а `cmd/hop` оставался
заглушкой этапа 2. Место — сразу после этапа 7, потому что связывать раньше
нечего, а всё, что идёт после, связку уже предполагает.

**Что делается.** Пакет `internal/agent` (§3.4): владение модулями, машина
состояний агента, проводка событий — переключение узла → сброс кэша резолвера,
смена сети → форс-проверка, отказ диалера → `health.ReportFailure`. Две фазы §2
и их сведение в `status`. Кольцо событий на 256 и `last_switch` в сторе.
Пересборка инстанса Xray при смене набора узлов с дренажом старого (§5.8).
`cmd/hop` худеет до разбора аргументов.

**Здесь впервые выполняется `hop up` в смысле §1/С3.** До этого этапа он не
выполнялся ни разу — ни в тестах, ни руками.

**Чем машина проверяет.** T30 (обновление подписки не рвёт живое соединение).
Тесты машины состояний агента на фейковых часах — L1, без прав, на трёх ОС.
Сквозной L2: поддельный `PacketDevice` плюс настоящие VLESS-инбаунды, полный
путь от пакета до узла и обратно, включая смерть узла и переключение под
нагрузкой. Резолвер на этом этапе ещё заглушка — его L2 приезжает этапом 6.

**Что должно сломаться при выключении.** `xray_drain=off` → T30 краснеет:
обновление подписки рвёт соединение вопреки §5.5. `phase_split=single` → тест,
проверяющий одновременные `orphaned` и `bypass`, теряет одну из половин.

**Что способен опровергнуть.** §3.4 целиком: связка — единственное место, где
границы модулей встречаются друг с другом, и если проводка событий потребует
протащить `store` в `netstack` или `engine` в `health`, значит границы проведены
не там. Также §5.8 в части дренажа: если 30 секунд окажется мало для типичного
соединения, порог придётся защищать числом, а не рассуждением.

**Что опроверг на самом деле — вторым заходом.** Первая редакция этапа С
объявила себя законченной, не тронув `cmd/hop`: связка была написана и зелена, а
вызывающего у неё не появилось. Собранный `hop` не содержал ни одного символа
Xray, netstack, `agent` или `store`; датаплейном оставался `drain` этапа 2, а
`hopd` при этом заворачивал в туннель весь трафик машины. Опровергнуто этим не
§3.4, а само правило «этап кончается, когда его пакеты зелены»: у этапа,
кончающегося бинарём, обязана быть проверка **на бинаре**, и она не бывает L1.
Регистр связки дополнен §5.8 — W36–W43, — а `cmd/hop` дописан до конца:
транспорт над `ipc`, агентское устройство поверх дескриптора, пробер §5.4 через
outbound (§6.7), привязка исходящих сокетов (§6.8) и ввод узлов флагами (Р40).

**Что опроверг на самом деле.** Не §3.4 — границы выдержали, и `internal/agent`
не понадобилось ни одного встречного импорта. Опровергнуто другое: **§5.8 не
выполнялся вовсе**, и это не было видно ни из спеки, ни из зелёного этапа 5.
`health.Manager` принимал узлы только в `New`, то есть при обновлении подписки
живость пришлось бы пересоздавать, обнуляя окно и `rtt` всех узлов, — ровно ту
историю, ради сохранения которой §5.8 отказывается от полной замены. Стор
сохранял бы `id` на диске, а автопереключение начинало бы с нуля каждые
несколько часов. Добавлен `Manager.SetNodes`; это единственная правка зелёного
этапа 5 за весь этап.

Первая редакция `SetNodes` внесла гонку данных — мутировала структуру, которую
проба читает вне замка, — и её нашёл `-race`, а не чтение. Подробности и обе
починки — в регистре, §9.

---

## Этап 8. Реальный TUN и платформенный хвост (L3) — Linux-ядро закрыто

**Что уже сделано.** Linux поднимает настоящий TUN; в netns проходят T21, T22,
T23-fast, T23-slow, T23b, T24, T29 и T31 — то есть не весь набор §8.4. T25
(петля), T26 (перехват :53), T27 (fail-close на уровне TUN) и T28 (IPv6) на L3
не написаны: поведение T25–T27 покрыто ниже уровнем, T28 не закрыт вовсе (см.
«Что осталось»).
Вместо UID-правила каждый новый TCP- и UDP-сокет агента привязывается к
физическому интерфейсу. `internal/outbound.Selector` следит за default route в
main table через rtnetlink, `net.Dialer.Control` ставит `SO_BINDTODEVICE`, а
ошибка binding закрывает соединение fail-closed. Через тот же путь идут Xray,
health-пробы и HTTP-подписки. `AgentUID` удалён; cleanup старого правила 31500
оставлен только для безопасного обновления. Первый непривилегированный bind
проверен ядром и защищён W36 и отдельным kernel-тестом.

Разрыв активных TCP-соединений при смерти узла (§5.5) тоже сделан: `tcpStack`
(`internal/netstack/tcp.go`) ведёт реестр открытых релеев, запись встаёт в
`pipe` и снимается тем же `stop()`, которым релей закрывается сам.
`Stack.InterruptTCP()` рвёт всё, что сейчас открыто, и возвращает число;
`Agent.InterruptConnections()` — провод в `health.Config.Interrupt`,
подключённый в `cmd/hop/main.go` тем же приёмом отложенного замыкания, что и
`Prober`. Проверено настоящим TCP-рукопожатием через gvisor против настоящего
`tcpStack.pipe` — `internal/netstack/interrupt_test.go`, чего не делали ни A18
(health, стенд), ни T9/T10 (l2, тоже стенд). Закрывает W8 и W14
(`docs/verification-agent.md:310,321`). UDP в реестр не входит — SPEC и регистр
(T9, T10, W8) говорят про TCP; расширение `natTable` тем же приёмом осталось
несделанным.

**Что осталось.** `BypassSink` стал реальным NAT-путём через тот же
`outbound.Selector` (`internal/bypass`, провод в `internal/agent/wire.go` и
`internal/netstack/netstack.go`) — только UDP; TCP в локальную сеть больше не
дроп, а RST: `reject.RST` (тот же построитель, что у `Reply`, но без вопроса
`Excluded`, который молчит на всю локальную сеть) под флагом
`bypass_tcp_reject`, T33 в §8.3. UDP, отвергнутый приёмником, ICMP не получает —
довод в `implementation-notes.md`. Списки bypass/block §6.10 из кода ушли: `netstack.Routing` —
два списка правил «префикс + протокол + порт», `netstack.Config.Routing` и
`agent.Config.Routing` (пусто — умолчания §6.10), исключения §5.6 из
конфигурации не вынимаются, обнаружение служб — вынимается. Смена интерфейса
доведена и до сокетов bypass: NAT помнит, к какому интерфейсу `Control`
привязал сокет, сверяет его с нынешним ответом `Selector.Interface` на каждом
`Send` и закрывает разошедшиеся — `bypass_rebind`, «следствие первое» §6.8.
Наблюдателя rtnetlink это не тронуло: сигнал берётся опросом уже публикуемого
`Interface()`, связка лишь пробрасывает `Config.Physical`, который у неё и так
был. Счётчики приёмника bypass (сокеты, осиротевшие ответы, передозвоны) видны
теперь в `Stack.Stats()`: приёмник остался `Send`-only, счётчики приезжают
необязательным интерфейсом `BypassStatSource` и проверкой типа, как знание о
транспорте у `StreamResolver`. Заодно `Stats.Rejected` перестал складывать в
одно число fail-close и отказ TCP на пути bypass — вторая половина выделена в
`BypassTCPRejected`, — а `bypass.NAT.Stats()` стала чистым снимком: уборка
простоя едет только на `Send`, потому что геттер, меняющий наблюдаемое, — это
поведение, и ему полагался бы свой флаг. Нужно закрыть IPv6 — политику `ipv6_block` и T28, — дописать
недостающие L3-тесты T25, T26 и T27, довести списки §6.10 до диска и CLI, а
затем реализовать и прогнать платформенные ветки macOS и Windows, включая Wintun
и проверенную поставку `wintun.dll`.

Пользовательские split-префиксы, когда они появятся, идут в правила
маршрутизации, а не в вердикт `bypass`: bypass выдаёт пакет через сокет агента и
потому является NAT, а не прозрачным форвардингом.

**Чем машина проверяет.** Linux — на каждый PR в netns T21, T22, T23-fast,
T23-slow, T23b, T24, T29 и T31: шаг `l3-linux` в `.github/workflows/ci.yml`
гоняет весь бинарь `internal/l3`, а недостающих T25–T28 в нём нет. Плюс W36 и
kernel-тест binding, `internal/netstack/interrupt_test.go` на каждый прогон L2.
L3-тест bypass построен: `internal/l3/bypass_test.go`,
`TestBypassMDNSReachesPhysicalInterfaceAndReturnsViaNAT` (T31, §8.4) —
mDNS-запрос из туннеля виден на физическом интерфейсе, ответ возвращается
клиенту через NAT. Списки §6.10 из конфигурации проверяет T32 (§8.3) плюс пять
тестов вердикта в `internal/netstack/routing_test.go`. Отказ вместо дропа на
пути bypass — T33 (§8.3) плюс два теста шва между `reject.Excluded` и вердиктом
`Bypass`: `TestBypassTCPNeedsRSTBecauseReplyStaysSilent` (`internal/netstack`) и
`TestRSTAnswersWhereReplyStaysSilent` (`internal/reject`). Перепривязку
сокетов bypass проверяют `TestBypassInterfaceChangeRebindsSocket`
(`internal/bypass`) и W47 `TestBypassSocketRebindsOnInterfaceChange`
(`internal/agent`, шов `Config.Physical` → `bypass.Config.Interface`); рядом с
ними `TestBypassIdleSweepNeverCollectsSocketUnderTraffic` закрепляет, почему
уборка простоя на эту роль не годится. Подмешивание счётчиков bypass в снимок
стека проверяет W48 `TestStackStatsSeesRealBypassNAT` (`internal/netstack`,
настоящий `bypass.NAT` за стеком — проверка типа промахивается молча, нулями)
и рядом с ним два теста на подставном приёмнике. macOS и Windows по расписанию пока только
собираются: `l3-macos.yml` и `l3-windows.yml` — скелеты, делающие `go build` и
`go vet` и печатающие, что привилегированный слой ещё не написан. Порядок «T22 и
T23-slow первыми» — план на то, когда эти ветки появятся.

**Что должно сломаться при выключении.** Отсутствие binding → W36; T25, который
должен закрывать ту же петлю на L3, ещё не написан. `ipv6_block=off` → T28 —
обещание на будущее, а не действующая проверка: ни политики, ни теста пока нет,
IPv6 сегодня умирает в ветке `!ok` разбора (`netstack.go`), а не намеренной
блокировкой. `routing_lists=off` → T32 и два теста вердикта из
`routing_test.go` краснеют, а T17 и T31 остаются зелёными: умолчания совпадают
с прежним жёстким набором. `bypass_sink=off` →
`TestBypassSendsDatagramFromBoundSocket` и `TestBypassReplyReturnsToClient`
(`internal/bypass`) и T31 (L3) краснеют, а T17 остаётся зелёным, потому что он
проверяет только вердикт, не доставку. `bypass_tcp_reject=off` → T33: RST не
строится, и тест ждёт полный `WaitTimeout` — то самое молчание, которое §5.6
запрещает; `TestBypassSendErrorCountsAsBlocked` при этом зелен при любом её
состоянии, потому что UDP-путь она не трогает. `bypass_rebind=off` →
`TestBypassInterfaceChangeRebindsSocket` (`internal/bypass`) и W47
(`internal/agent`) краснеют: сокет после смены интерфейса остаётся прежним, и
это видно по неизменившемуся исходному порту у слушателя. Флаг не слит с
`bypass_sink`: тот гасит путь целиком, и под ним обе проверки покраснели бы по
чужой причине — «наружу вообще ничего не ушло», а не «сокет не сменился».
Без реестра релеев
`TestInterruptTCPClosesLiveConnection` и его соседи в `internal/netstack` не
собираются вовсе — они падают на этапе компиляции, если `Stack.InterruptTCP`
убрать, а не только на ассерте.

**Что способен опровергнуть.** Нижнюю границу поддерживаемых Linux-ядер для
первого непривилегированного `SO_BINDTODEVICE`, корректность NAT-пути bypass и
платформенные стратегии macOS/Windows. Возврат к UID-правилу не является
запасным вариантом: общий desktop UID исключил бы из туннеля весь трафик
пользователя.

---

## Этап 9. CLI, события, инсталляторы (L4)

**Что делается.** Сокет с рассылкой нескольким клиентам (§3.3) поверх готового
`internal/ipc` — свой набор Op, не второй протокол. Подкоманды
`up/down/status/nodes/events/bypass/sub/node/auto/autoconnect` и `hop agent` как
фоновый режим того же бинаря.

**Здесь же снимаются флаги ввода этапа С.** `hop -sub`, `hop -node`,
`hop -nodes` (Р40 регистра связки) — временный интерфейс: без какого-либо ввода
этап С был непроверяем вовсе, узлам неоткуда было взяться. Подкоманды надеваются
поверх тех же вызовов — вся работа делается пакетами `subscription` и `store`, а
не флагами, — поэтому переписывается здесь разбор аргументов, и только он. Флаги
объявляются устаревшими и удаляются в том же этапе: два интерфейса к одному
стору разойдутся. `--json` у всех читающих команд, коды возврата 0/1/2/3.
Инсталляторы: `.deb` и `.rpm` из одного конфига плюс unit, `.pkg` с
LaunchDaemon, MSI со службой и `wintun.dll`.

До этого этапа допустим только локальный dev-install для запуска `hop` по имени
и регистрации `hopd`; штатные release-пакеты остаются здесь, после стабилизации
CLI и событий.

**Чем машина проверяет.** I1–I5; тест на двух одновременных клиентах; тест кодов
возврата, где fail-close даёт 3, а недоступный агент — 2.

**Что должно сломаться при выключении.** `event_broadcast=single` → тест на двух
клиентах краснеет. `json_schema=off` → I5 краснеет. `exit_codes=binary` → тест
кодов перестаёт отличать 3 от 1.

**Что способен опровергнуть.** §4 в части подписи: если `sudo installer -pkg`
или `msiexec /i` на актуальных версиях ОС всё же упрутся в Gatekeeper или
SmartScreen, неподписанные пакеты в v1 не поедут, и либо возвращается tarball,
либо подпись въезжает в v1 вопреки §4. Проверяется I1 на чистой машине — то есть
до релиза, а не на пользователе.

---

## Открытые вопросы §7 — привязка к этапам

Нумерация — по §7 в его нынешней редакции (прежняя содержала два пункта «3»).
Блокеров не осталось: оба закрыты.

| №   | вопрос                                    | этап | блокирует?                                                                                                               |
| --- | ----------------------------------------- | ---- | ------------------------------------------------------------------------------------------------------------------------ |
| —   | Windows: распространение `wintun.dll`     | 8    | Был блокером, **закрыт**: тянем при сборке из официального архива со сверкой SHA-256 и вендорской подписи, кладём в MSI. |
| —   | Пропускная способность границы на Windows | 1    | **Закрыт** этапом 1: измерено бенчмарком B1.                                                                             |
| 1   | Лицензия продукта (GPL-3.0 через `sing`)  | —    | Нет для кода. Решать до первого публичного релиза, то есть до этапа 9 включительно; на этапы 6–8 не влияет.              |
| 2   | Captive portal                            | 9    | Нет. `hop bypass --on` из §1/С6 закрывает сценарий; автодетект — решение после релиза.                                   |
| 3   | Квоты (`Subscription-UserInfo`)           | 7    | Нет. Поле уже лежит в `Group.quota_info`; показывать и исключать ли узел — решается на готовом сторе.                    |
| 4   | Роутинг по доменам                        | —    | Нет, вне v1 (§4). Смягчён собственным резолвером этапа 6: маппинг домен→IP известен нам.                                 |

---

## Где план расходится со спекой

**D1. §6.2 переписан.** Одного порога heartbeat недостаточно: он не может
одновременно удовлетворить T23 и T24, и внутри его окна пакеты дропаются молча
вопреки §5.6. Замена — ребро по закрытию соединения (0 мс), responder на TUN и
один `orphan_deadline`. Heartbeat остаётся, но только против заклинившего живого
агента. Подробно — раздел 0.

**D2. §2: `TunnelState.phase` получает значение `orphaned`.** Прямое следствие
D1; перечисление `down|starting|up|failing|bypass` не может выразить это
состояние, а `status` обязан его показывать.

**D3. T23 расщеплён на T23-fast / T23-slow, добавлен T23b.** «≤ N с» в спеке —
это два разных свойства (отказ и уборка) с разными порогами. T23b покрывает
случай, для которого в спеке нет теста вообще.

**D4. Порог B1 на Linux/macOS снимается; гейтом остаётся только Windows.**
_Расхождением быть перестало: §8.6 переписан по этому пункту, и спека теперь
говорит то же самое._ §4 исключает batch read/write, и на Unix это не временное
упрощение, а единственная возможность: `tun_linux.go:451` без `vnetHdr`
возвращает один пакет за `read()`, `tun_darwin.go:306` `BatchSize()` захардкожен
в `1`, а единственный путь к большему — `IFF_VNET_HDR` + `TUNSETOFFLOAD`, то
есть GSO из исключённого списка §4. Порог 2 Гбит/с гейтил бы не §5.3, а скорость
gvisor и syscall'ов, и упирал бы проект в оптимизацию, которую §4 сам вынес за
v1. Linux и macOS меряются как базовая линия; отдельно снимается контрольный
замер с `IFF_VNET_HDR`, чтобы знать цену возврата GSO.

**D5. B1 в спеке меряет только трубу; реальный путь на Windows добавляет memcpy
из кольца Wintun.** `ReceivePacket` отдаёт срез в кольцо, который надо
освободить. План меряет B1a (труба) и B1b (кольцо + труба); гейтом §5.3 служит
B1b. _Расхождением быть перестало: §8.6 переписан, оба замера и выбор гейта
теперь в спеке._

**D6. Реестр флагов и негативная CI-задача строятся на этапе 0**, до появления
первой политики. §8 формулирует правило, но не размещает под него работу.
Ретрофит флагов в готовый код — ровно то место, где покрытие тихо перестаёт быть
полным.

**D7. Порядок этапов не следует порядку §8.2 → §8.3.** T12–T17 (L2) приезжают
раньше всех, на этапе 3: health им не нужен. Деление на L1/L2/L3 — матрица
запуска, а не порядок сборки.

_Уточнено по факту._ Изначально план вёз T1–T8 и T9–T11 вместе на этапе 5. Так
не вышло, и к лучшему: L1-часть этапа 5 (`internal/health`, T1–T8 и A1–A33)
собралась целиком до этапа 4, на фейковом пробере и фейковых часах, — политика
живости не нуждается в Xray, чтобы быть проверенной. L2-хвост (T9, T10, T11,
T20, A34, A35, B2) остался ждать инжектора и переехал в этап 4. Итог: этап 5
кончился раньше этапа 4 и без единой строчки заглушек, а §6.7 остался
недоказанным до появления настоящего outbound — это записано в регистре явно.

**D8. Исключения §5.6 должны действовать и в `orphaned`.** §5.6 объявляет их для
fail-close, §6.2 молчит. TUN-responder должен сохранять DHCP/NTP и локальные
исключения явно; иначе машина теряет DHCP-аренду именно в окне orphaned.

**D9. §8.4 требует, чтобы на Windows первым шёл T22.** С учётом D1 этого мало:
T23-slow восстанавливает то же состояние, но из другого исходного, и сломанный
`orphaned`-teardown оставит машину без сети так же. План гейтит Windows-прогон
парой T22 + T23-slow.

**D10. §6.15 задаёт перечисление, но не задаёт место классификации.** План
закрепляет его за одной функцией-обёрткой `core.Dial` и запрещает линтом вызов
`health.ReportFailure` из любого другого пакета. Без этого сбывается опасение,
записанное в самом §6.15: классификация расползается по коду и однажды начинает
считать смертью упавший сайт.

**D11. Добавлен T29 — смерть самого сервиса.** Спека покрывает смерть агента
(T23) и его перезапуск (T24), но не смерть владельца маршрутов. Живой Linux-тест
показал: интерфейс уходит, а правила Hop 31000/32000 остаются; старое
UID-правило 31500 тоже могло пережить сервис до отказа от этой схемы. Поэтому
стартовый `Reclaim` обязан чистить осиротевшие правила. На macOS и Windows
дополнительно проверяются настройки DNS. Место — этап 2.

**D12. Добавлен этап С — связка.** §3.4 перечисляет `supervisor` наравне с
остальными модулями, но первая редакция плана не отводила ему ни этапа, ни
проверок: предполагалось, что модули соединятся сами собой в этапе 9. Так не
бывает. Связка — единственное место, где встречаются границы всех шести модулей,
и ошибка в ней стоит переписывания остальных. Место — сразу после этапа 7.
Заодно §3.4 переименовал `supervisor` в `agent` и дал ему владение модулями:
логика, живущая в `main`, не проверяется без бинаря и без прав.

**D13. Порядок был изменён на 7 → С → 6 → 8 → 9.** Резолв «через активный узел»
(§5.7) было нечем проверить без набора узлов и живого движка. Этапы 7 и С теперь
закрыты, Linux-ядро этапа 8 также доказано; текущий хвост — 6 → 8 (остаток) → 9.
Номера этапов при этом остаются именами, а не очередью.

**D14. §2 разделил `phase` на две.** Перечисление
`down|starting|up|orphaned|failing|bypass` смешивало владельцев: первые четыре
значения знает только сервис, последние два — только агент, и одно общее поле
потребовало бы либо дублирования, либо того, чтобы сервис узнал про живость
узлов (против §5.2). Побочная находка: значения для стартового бюджета §5.6 в
прежнем перечислении не было вовсе, хотя само состояние спека описывает и
отводит ему 30 секунд. Добавлено как `traffic_phase: waiting`.

**D15. §6.8 переписан по измерению: binding вместо UID-правила.** Все сокеты
узлов, health, подписок, bootstrap и bypass привязываются к текущему физическому
интерфейсу на каждой ОС. На Linux первый `SO_BINDTODEVICE` доступен
непривилегированному процессу на проверенном ядре, тогда как `SO_MARK` закрыт;
ошибочное предположение о `CAP_NET_RAW` снято kernel-тестом. UID-правило
отвергнуто: общий desktop UID уводил бы мимо туннеля весь пользовательский
трафик. Binding действует на каждый новый сокет; смена сети требует обновить
selector и переподключить старые соединения. Bypass остаётся NAT, а не
прозрачным форвардингом.

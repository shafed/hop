package store

// syncDir на Windows не делает ничего: каталог там не открывается как файл и
// FlushFileBuffers на нём не работает, а долговечность самого переименования
// обеспечивает MoveFileEx. Пустая реализация здесь честнее заглушки с ошибкой:
// шаг не пропущен по недосмотру, его на этой платформе нет.
func syncDir(string) error { return nil }

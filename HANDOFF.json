{
  "status": "machine-readable — only for the next AI session, not for human reading. Human-readable history/decisions live in PLAN.md and implementation-notes.md.",
  "convention": "This file carries POINTERS AND TRAPS, not retellings. A задача is cited by its section in PLAN.md or SPEC.md, never paraphrased here: a paraphrase is a second copy of the truth and it drifts (it already did once, for routing_lists). What belongs here instead: file and symbol pointers, measured facts, rejected shapes with their arguments, and the traps that cost the last passes time. Written down in full in implementation-notes.md, section 'doclint — утверждение документа о состоянии обязано опираться на исполнимое'.",
  "branch": "main",
  "branch_note": "The ipv6_kill_established pass is four implementation/documentation commits ending at 808712f, based directly on 9cca057 = main at the start of the pass, followed by this handoff update. It was independently rechecked and fast-forwarded into local main. Nothing was pushed.",
  "citation_hygiene": {
    "rule": "Every path, T/W number, policy name and git ref named in this file must exist. This is no longer a convention a human has to remember — `go run ./cmd/doclint .` checks it, and CI runs it as `go run ./cmd/doclint -git=false .`.",
    "why_this_block_still_exists": "It records what the check is FOR. Two consecutive passes cited 'Global Constraint 9, constraints.md' — a file that never existed in any branch — and the citation was what justified deferring a task that, when finally checked on its merits, cost one optional field and one wiring line. The rule that citation stood in for: a pass closes its own queue item and does not widen into neighbouring modules; where the correct shape does require changing another package, that is recorded in Deviations with an argument — it is not grounds to defer the task silently.",
    "exceptions_list": "docs/not-yet-written.md — the single list doclint reads. It goes red in BOTH directions: a line that has become unnecessary is a finding too, so the list cannot rot into a dumping ground."
  },
  "plan_order": "9 (remainder) — Linux dataplane closed except T25-T27; §6.10 lists and the platform branches are what is left",
  "closed_this_pass": [
    {
      "what": "Surface bypass.NAT.Stats() through netstack.Stack.Stats() (the previous HANDOFF's next_task, priority 1)",
      "head": "449ba62",
      "decision_log": "implementation-notes.md, section 'Этап 9 — счётчики bypass в снимке стека (`Stack.Stats`)'",
      "shape": "BypassSink stayed Send-only; the counters arrive through an OPTIONAL interface BypassStatSource { Stats() bypass.Stats } and a type assertion in Stack.Stats(). Load-bearing argument: the ability belongs to whoever has sockets, the obligation would have fallen on everyone — including fakenet.Bypass, which deliberately depends on neither testing nor product packages. Same device as StreamResolver/FlushableResolver, in the same file.",
      "price_named_out_loud": "A missed type assertion is silent — it reports zeros. That is why the registered guard (W49) puts a real bypass.NAT behind the stack rather than a fake sink.",
      "two_side_decisions": [
        "The idle sweep was REMOVED from bypass.NAT.Stats(): a getter that changes the observed is behavior, and behavior owes a flag. Price: with traffic stopped, bypass sockets now live until Close — see next_task priority 3.",
        "Stats.Rejected no longer sums fail-close refusals with bypass-TCP refusals; the second half is BypassTCPRejected. The sum is preserved and no existing assertion moved."
      ],
      "registry": "docs/verification-agent.md §5.8 row W49 (TestStackStatsSeesRealBypassNAT). No flag: an observability fold nothing branches on is not a policy."
    },
    {
      "what": "IPv6 blocking under ipv6_block (queue item 1; SPEC §6.9). T28 written and green in netns",
      "head": "ca7a096",
      "decision_log": "implementation-notes.md, section 'Этап 8 — блокировка IPv6 (`ipv6_block`, T28, W48)' — six measurements, the mechanism, the policy border, four Deviations",
      "measured_not_assumed": [
        "With the tunnel up, IPv6 leaves through the PHYSICAL interface and the connection to an outside peer establishes. The tunnel table is empty for IPv6 and never participates in the lookup, because Up only ever ran `ip route add` — an IPv4-family command. So the leak goes PAST the TUN, and the !ok branch of netstack parsing has nothing to do with it: a packet that never reaches the device is a packet the stack never sees.",
        "unreachable → ENETUNREACH, prohibit → EACCES, blackhole → EINVAL. unreachable chosen: blackhole is silence, which §5.6 forbids, and ENETUNREACH is the signal Happy Eyeballs falls back to IPv4 on without delay.",
        "sysctl disable_ipv6 was rejected BY MEASUREMENT: it destroys global addresses and does not bring them back, so the snapshot diverges by three lines — the mechanism breaks §8.4 by construction.",
        "The netstate snapshot did not see IPv6 rules at all (`ip -o rule show` is IPv4-only). A rules6 section was added; verified by the reverse experiment — Reclaim without its -6 half drops T29."
      ],
      "spec_correction": "SPEC §6.9 said IPv6 'is blocked at the level of TUN routes'. Measurement disproved it; the section is rewritten. This is the project's standing rule at work: the measurement wins, the spec is edited.",
      "policy_border": "ipv6_block switches exactly one step of Up: `ip -6 rule add unreachable priority 32000`, installed FIRST and therefore removed LAST (the journal unwinds LIFO), so there is no moment where the tunnel is up and IPv6 is open. Not switched: rules6 in netstate (§8.4 contract) and the -6 half of Reclaim (a startup obligation, §6.2) — neither branches behavior, so neither could go red.",
      "registry": "docs/verification-agent.md §5.8 row W48 (TestW48UpBlocksIPv6, privilege-free: the list of Up steps is a pure function, upSteps) and §6 flag row ipv6_block. T28 lives in internal/l3 and is NOT named in the policy registry, by the standing rule: negcheck runs `go test`, and L3 skips without HOP_L3, so it would be green under any state of the flag — exactly the fixture_bad shape."
    },
    {
      "what": "Refuse, don't silence, IPv6 TCP connections that predate `up` (ipv6_kill_established, W50)",
      "head": "808712f",
      "decision_log": "implementation-notes.md, section 'Этап 8 — отказ соединениям IPv6 старше туннеля (`ipv6_kill_established`, W50)'",
      "shape": "Immediately after the ipv6_block rule, Linux dumps TCP6 sockets with inet_diag and destroys eligible pre-existing sockets via a hand-serialized SOCK_DESTROY request. It repeats while a round destroys anything, at most four rounds, to narrow the connect()/diag race. `ss --kill` was rejected by measurement: it returns 0 on EPERM and its default TCP state filter misses SYN_SENT.",
      "boundary": "Only non-local, non-listening, genuine IPv6 sockets are targets; own addresses, ::1 and IPv4-mapped sockets are left alone. The step exists only when BOTH ipv6_block and ipv6_kill_established are on. It has no rollback because socket destruction is deliberately irreversible and sockets are outside the netstate snapshot.",
      "degradation": "A kernel without INET_DIAG_DESTROY does not bring Up down: the blocking rule is already installed, so confidentiality is preserved; hopd logs a Warn with the reason and the count left alive. This loses prompt refusal, not the block.",
      "registry": "docs/verification-agent.md §5.8 row W50 (TestW50UpSweepsSilencedIPv6) and §6 flag row ipv6_kill_established. TestW50IPv6EstablishedIsAbortedNotSilenced is the real-kernel half and deliberately stays outside the registry because it skips without HOP_L3.",
      "post_pass_verification": [
        "Full `go test ./...` passed outside the network-restricted sandbox; the sandbox-only run failed solely because internal/engine could not listen on 127.0.0.1:0.",
        "The affected-package race run passed; doclint is clean; W50 is green normally, red with HOP_DISABLE=ipv6_kill_established, and green (skipped by policy boundary) with HOP_DISABLE=ipv6_block.",
        "The real W50 L3 test passed in unshare -Urn: hopd destroyed two sockets; ESTABLISHED and SYN_SENT both returned ECONNABORTED in microseconds.",
        "Full negcheck was run after the report, outside the network-restricted sandbox: all 41 policies are guarded by a red test. The first sandboxed attempt produced only eight oracle defects from forbidden loopback listen calls and is not a product result.",
        "The report's IPv4 follow-up was measured correctly on 203.0.113.2, outside LocalPrefixes. In 5/5 isolated-netns runs the post-up route was hopt0/table 8421 and the pre-up direct TCP connection received an immediate RST; it never continued directly. No IPv4 SOCK_DESTROY policy is justified by this scenario. The temporary measurement test was removed and the worktree was clean afterward."
      ]
    },
    {
      "what": "doclint — a document's claim about state must rest on something executable (the parallel task; not part of plan_order)",
      "head": "09af0d2",
      "decision_log": "implementation-notes.md, section 'doclint — утверждение документа о состоянии обязано опираться на исполнимое', including where the recognition boundary was drawn and why",
      "shape": "internal/doclint (logic) + cmd/doclint (thin wrapper, like negcheck). Four checks: a T*/W* number must exist as a Go test (the number in the Test* function name or in its doc comment); a policy name must be registered in internal/policy; a path must exist; a branch or commit named in HANDOFF.json must resolve in git (-git=false switches that half off, and CI uses it: a checkout has neither the pass branches nor the full history).",
      "recognition_boundary": "Deliberately conservative — a linter that shouts at every other word gets switched off. Paths are read only out of backticks (in JSON, out of string values) and only when the first segment is an existing top-level entry: one condition drops every quotation from refs/ and every import. A bare filename WITH a line reference is not checked at all (that is how foreign code is cited; own code is cited by full path). .json is excluded from extensions — nodes.json/health.json live on the user's disk. A policy is recognised only as `name=` or as a table cell containing nothing else.",
      "red_before_fixes": "34 findings, including all three known true positives (ipv6_block unregistered, T25-T28 missing, the stale branch block in this very file). Fixed by writing docs/not-yet-written.md where the absence is honest, and by correcting the document where the claim was simply false (PLAN.md pointed at this very file under a name it has never had; T31 existed but carried no number in its doc comment).",
      "no_flag": "A linter is not a product policy, and realtimelint carries none either. Argued explicitly in implementation-notes.md rather than left implicit."
    }
  ],
  "parallel_passes_trap": {
    "what_happened": "Two of the three passes independently took the next free registry number and both landed W48. Nothing catches this inside a single pass: each read the registry, saw W47 as the last row, and was right at the time it looked.",
    "resolved_how": "0e1a198. W48 stayed with ipv6_block, whose tests carry the number in their NAMES (TestW48UpBlocksIPv6); the snapshot guard became W49 and got its number into the doc comment of TestStackStatsSeesRealBypassNAT — which doclint then demanded, because a registry number with no test behind it is exactly its first check.",
    "rule_for_the_next_parallel_run": "Before a parallel pass writes a registry row, the controller hands out the numbers — or every pass writes its number into the test itself, where the collision is a compile-time-visible fact rather than a merge surprise."
  },
  "next_task": {
    "priority": 1,
    "name": "Довести Stack.Stats() до пользователя",
    "why_first": "The counters already reach netstack.Stack.Stats() but stop there: the agent forwards only DNSStats, and neither ipc nor cmd/hop exposes the stack counters. Rebound and the new IPv6 sweep outcome are currently observable only in tests or hopd logs.",
    "shape_question": "Decide whether counters join agent.Snapshot (docs/verification-agent.md §2 says a snapshot in pieces is not a snapshot) or travel through a separate call like Agent.DNSStats() in internal/agent/wire.go and cmd/hop/watch.go. Include the IPv6 sweep count and unsupported-kernel degradation only if their ownership and lifetime fit the same snapshot; do not silently turn a log event into a cumulative counter without specifying reset semantics.",
    "flag": "probably none — observability does not branch product behavior"
  },
  "queue": [
    {
      "task": "Уборка простоя сокетов bypass без трафика",
      "why": "The price the stats pass named out loud: with traffic stopped, bypass sockets live until Close. natTable has the same fate for the same reason — neither table has a background ticker.",
      "shape_hint": "A ticker on clock.Clock, or an external sweep (natTable has Sweep() for this; bypass.NAT does not). This IS behavior: a flag and a red-first guard are obligatory.",
      "flag": "needed"
    },
    {
      "task": "§6.10 lists from disk and CLI",
      "detail": "netstack.Routing and agent.Config.Routing exist and are honored, but nothing outside Go code fills them: cmd/hop/main.go never sets Config.Routing and internal/store has no settings file at all. Closing this means an on-disk format, a CLI surface (§5.9) and validation of user-supplied prefixes and ports — a store question, not a verdict question.",
      "note": "agent.Config.DNSUpstreams has exactly the same missing half and the same shape; solve both at once or neither."
    },
    {
      "task": "T25, T26, T27 on L3",
      "detail": "The three L3 tests listed in docs/not-yet-written.md with their reasons. All three wait on the same thing — a stand with a second interface. T28's stand (internal/l3/harness_test.go) is now most of it.",
      "note": "The property behind each is already held one level down (W36 for T25; T14/T16 for T26; T12/T13 for T27), so this is coverage at the kernel level, not an open hole."
    },
    {
      "task": "macOS dataplane",
      "detail": "utun via AF_SYSTEM, SIOCAIFADDR, PF_ROUTE",
      "note": "Two Linux-shaped mechanisms must be re-answered there, not ported: bypass_rebind compares interface NAMES, so outbound.Selector on macOS/Windows must publish the same thing (internal/outbound/selector_other.go is the stub), and ipv6_block is an `ip -6 rule` — the other platforms need their own way to refuse IPv6 while the tunnel is up, and refusing is not optional (§5.6)."
    },
    {
      "task": "Windows dataplane",
      "detail": "Wintun + wintun.dll delivery — §7 already resolved: pull from the official archive at build time, verify SHA-256 and vendor signature.",
      "note": "platform.New() explicitly errors on both OSes today — correct behavior; a silent stub would give a green L3 with nothing actually checked. The .github/workflows/l3-macos.yml and l3-windows.yml jobs are skeletons that build and vet, and say so."
    },
    {
      "task": "Свести §6.1 регистра и docs/not-yet-written.md в один источник",
      "why": "There are two lists of what is deliberately unwritten: the prose paragraph in docs/verification-agent.md §6.1 and the machine-readable one. The paragraph has already drifted — it counts W14 and W33 as unwritten while their tests exist. Either the paragraph shrinks to reasons plus a pointer, or doclint learns to check the reverse direction.",
      "size": "small, documents only"
    },
    {
      "task": "Дать номера регистра тестам, закрывающим W29-W31",
      "why": "§6.1 claims TestReportFailureHasOneCallSite covers them; the test carries no number, so three registry rows sit in the unwritten list although the check exists. Exactly the case T31 was in before doclint.",
      "size": "small"
    }
  ],
  "gate": {
    "commands": [
      "go test ./...",
      "go test -race <affected packages>",
      "go run ./cmd/doclint .",
      "go run ./cmd/negcheck",
      "go run ./cmd/realtimelint ."
    ],
    "notes": {
      "doclint": "New this pass. Fast. Runs in CI as `go run ./cmd/doclint -git=false .` — a checkout has neither the pass branches nor the full history, so the git half is off there and on locally.",
      "negcheck": "runs every guard twice (with/without its policy) via `go test -count=1`; 41 policies as of this pass, ~20-35 min wall clock on this machine — this is expected, not a hang; don't rely on go test's cache. It needs permission to listen on loopback: a network-restricted sandbox creates false oracle defects. Per-task gate only needs the new policy's own guard under HOP_DISABLE=<name>; the controller runs the full one at the end.",
      "realtimelint": "does not accept ./... as an argument — pass literally `.`",
      "l3_how_to_run": "Build once, run under an unprivileged user namespace: `go test -c -o /tmp/l3.test ./internal/l3` then `HOP_L3=1 unshare -Urn /tmp/l3.test` (add `-test.v -test.run '<pattern>'` to scope it). This works without root — unshare -Urn grants CAP_NET_ADMIN inside the fresh netns. Reusing the built binary across repeated unshare invocations is much faster than `go test` per run.",
      "known_red_that_is_not_yours": "go vet ./... is red at internal/engine/dialer.go (copylocks in xray-core's SocketConfig). `go run ./cmd/realtimelint .` is red with 3 hits in internal/dnstest/realserver_test.go. `gofmt -l ./internal` reports internal/l2/dns_test.go. All three predate these passes; the honest gate is 'no NEW findings', compared on the full output rather than the exit code.",
      "flake": "internal/l2 TestT10SlowNodeKeepsConnectionAlive occasionally flakes on timing under load; re-run it alone before recording it as a regression."
    },
    "rule": "every new policy must ship with a test that is red without it"
  }
}
